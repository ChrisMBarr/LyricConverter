import { FormatLyricConverter } from './format-lyric-converter';

describe('FormatLyricConverter', () => {
  it('should create an instance', () => {
    expect(new FormatLyricConverter()).toBeTruthy();
  });
});
