/**
 * @description A single character to use with the .split() method to separate multi-value string lists
 */
export const STRING_LIST_SEPARATOR_SPLIT = '|';

/**
 * @description A space padded character to use with .split() to save multi-value string lists with
 */
export const STRING_LIST_SEPARATOR_JOIN = ` ${STRING_LIST_SEPARATOR_SPLIT} `;

/**
 * @description A unique string used to differentiate custom friendly error message from native errors
 */
export const CUSTOM_ERROR_IDENTIFIER = '[[LyricConverter Custom Error]]';
