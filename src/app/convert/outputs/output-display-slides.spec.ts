import { OutputTypeDisplaySlides } from './output-display-slides';

describe('OutputDisplaySlides', () => {
  it('should create an instance', () => {
    expect(new OutputTypeDisplaySlides()).toBeTruthy();
  });
});
